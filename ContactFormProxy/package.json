{
  "name": "contact-form-proxy",
  "version": "1.0.0",
  "description": "AWS Lambda proxy to hide API key",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}